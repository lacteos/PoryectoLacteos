<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:ProyectoLacteos.ViewModel"
             x:Class="ProyectoLacteos.View.viewPedido">
    <ContentPage.BindingContext>

        <local:ViewModelPedido></local:ViewModelPedido>

    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Padding="30">


            <Label Text="Crear Pedido" HorizontalOptions="Center" FontSize="24" FontAttributes="Bold" Margin="0,0,0,20" />

            <Entry  Text="{Binding Id_Usuario}" Placeholder="ID Usuario" Keyboard="Numeric" Margin="0,0,0,10" />
            <Entry  Text="{Binding Id_Direccion}" Placeholder="Id Direccion" Keyboard="Numeric" Margin="0,0,0,10" />

            <Entry  Text="{Binding Estado}" Placeholder="Estado" Keyboard="Numeric" Margin="0,0,0,10" />

            <Button Command="{Binding crearPedido}"  Text="Crear Producto" BackgroundColor="#2196F3" TextColor="White" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>